//Prática perigosa = devido ao contexto de determinado interface, em alguns momentos vamos repetir
// só que em outros, será necessário alguma informação a mais ou a menos dependendo do componente
// Então pode ser que de mais trabalho separar em outro arquivo

export interface User {
  name: string;
  lastName: string;
}
